In terms of default import and export we can simply use export default functionName and we can import it import functionName from 'module.js' with no curly braces.

We can change the name of exports during import by using as keyword like import multiply as product.